# NCRRA brand asset handoff

Place the approved NCRRA mark at `assets/brand/ncrra_mark.png` before running the Flutter project. The React prototype uses the approved mark already available in the visual reference set; the mobile build must use that same approved source asset, with no redraw or substitute logo.
