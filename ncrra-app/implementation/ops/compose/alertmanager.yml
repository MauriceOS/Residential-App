global:
  resolve_timeout: 5m
route:
  receiver: operations-webhook
  group_by: [alertname, service]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: operations-webhook
    # Configure a protected operations webhook through target-host secret management before production.
    webhook_configs: []
